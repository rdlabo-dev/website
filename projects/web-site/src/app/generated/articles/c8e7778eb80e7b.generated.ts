// Generated by scripts/generate-articles.ts. Do not edit.
export default {"html":"<p data-line=\"0\" class=\"code-line\">Stencil, the Web Components library, ships with unit and E2E tests by default, like other JavaScript frameworks.</p>\n<p data-line=\"2\" class=\"code-line\"><a href=\"https://stenciljs.jp/docs/testing-overview\" target=\"_blank\" rel=\"noopener noreferrer\">https://stenciljs.jp/docs/testing-overview</a></p>\n<p data-line=\"4\" class=\"code-line\">E2E tests for Web Components often trip you up on Shadow DOM access. I struggled with how to change a select and fire an event, so I am sharing what worked.</p>\n<h2 id=\"component-under-test\" data-line=\"6\" class=\"code-line\"><a class=\"header-anchor-link\" href=\"#component-under-test\" aria-hidden=\"true\"></a> Component Under Test</h2>\n<p data-line=\"8\" class=\"code-line\">Suppose the component has a <code>select</code> DOM inside the Web Component, like this. In the E2E test I want to run <code>onChange</code> there and fire the <code>myChange</code> event. Let the component name be <code>my-select</code>.</p>\n<div class=\"code-block-container\"><pre class=\"shiki github-dark\" style=\"background-color:#151e2c;color:#e1e4e8\"><code class=\"code-line\" data-line=\"10\"><span class=\"line\"><span>@Component({</span></span>\n<span class=\"line\"><span>  tag: 'my-select',</span></span>\n<span class=\"line\"><span>  styleUrl: 'my-select.scss',</span></span>\n<span class=\"line\"><span>  shadow: true,</span></span>\n<span class=\"line\"><span>})</span></span>\n<span class=\"line\"><span>export class MySelect {</span></span>\n<span class=\"line\"><span>  ...</span></span>\n<span class=\"line\"><span>  private onChange = event =&gt; {</span></span>\n<span class=\"line\"><span>    this.myChange.emit({ value: event.target.value });</span></span>\n<span class=\"line\"><span>  };</span></span>\n<span class=\"line\"><span>  render() {</span></span>\n<span class=\"line\"><span>    return (</span></span>\n<span class=\"line\"><span>      &lt;Host&gt;</span></span>\n<span class=\"line\"><span>        &lt;select onChange={this.onChange}&gt;</span></span>\n<span class=\"line\"><span>          {options.map(option =&gt; (</span></span>\n<span class=\"line\"><span>            &lt;option value={option.value}&gt;{option.label}&lt;/option&gt;</span></span>\n<span class=\"line\"><span>          ))}</span></span>\n<span class=\"line\"><span>        &lt;/select&gt;</span></span>\n<span class=\"line\"><span>      &lt;/Host&gt;</span></span>\n<span class=\"line\"><span>    );</span></span>\n<span class=\"line\"><span>  }</span></span>\n<span class=\"line\"><span>}</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span></span></span></code></pre></div><h2 id=\"test-code\" data-line=\"36\" class=\"code-line\"><a class=\"header-anchor-link\" href=\"#test-code\" aria-hidden=\"true\"></a> Test Code</h2>\n<p data-line=\"38\" class=\"code-line\">One thing to watch when writing the test: Puppeteer's <code>select</code> method does not work with Shadow DOM.</p>\n<p data-line=\"40\" class=\"code-line\"><a href=\"https://github.com/puppeteer/puppeteer/issues/4171\" target=\"_blank\" rel=\"noopener noreferrer\">https://github.com/puppeteer/puppeteer/issues/4171</a></p>\n<p data-line=\"42\" class=\"code-line\">So use Puppeteer's <code>evaluateHandle</code> to reach the window and document objects and manipulate the DOM from there.</p>\n<p data-line=\"44\" class=\"code-line\">The test then looks like this.</p>\n<div class=\"code-block-container\"><pre class=\"shiki github-dark\" style=\"background-color:#151e2c;color:#e1e4e8\"><code class=\"code-line\" data-line=\"46\"><span class=\"line\"><span>describe('my-select', () =&gt; {</span></span>\n<span class=\"line\"><span>  it('change', async () =&gt; {</span></span>\n<span class=\"line\"><span>    const page = await newE2EPage();</span></span>\n<span class=\"line\"><span>    await page.setContent(`&lt;my-select&gt;&lt;/my-select&gt;`,);</span></span>\n<span class=\"line\"><span>    await page.waitForChanges();</span></span>\n<span class=\"line\"><span>    </span></span>\n<span class=\"line\"><span>    const change = await page.spyOnEvent('myChange');</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span>    await page.evaluateHandle(() =&gt; {</span></span>\n<span class=\"line\"><span>      return new Promise(resolve =&gt; {</span></span>\n<span class=\"line\"><span>        // Get the my-select DOM element</span></span>\n<span class=\"line\"><span>        const element = document.querySelector('my-select');</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span>\t// Resolve the async operation when the myChange event fires</span></span>\n<span class=\"line\"><span>        element.addEventListener('myChange', event =&gt; {</span></span>\n<span class=\"line\"><span>          resolve();</span></span>\n<span class=\"line\"><span>        });</span></span>\n<span class=\"line\"><span>\t</span></span>\n<span class=\"line\"><span>\t// Get the select DOM element inside my-select</span></span>\n<span class=\"line\"><span>        const select: HTMLSelectElement = element.shadowRoot.querySelector(</span></span>\n<span class=\"line\"><span>          'select',</span></span>\n<span class=\"line\"><span>        );</span></span>\n<span class=\"line\"><span>\t// Dispatch the event on the select DOM element</span></span>\n<span class=\"line\"><span>        select.dispatchEvent(</span></span>\n<span class=\"line\"><span>          new Event('change', { bubbles: true, composed: true }),</span></span>\n<span class=\"line\"><span>        );</span></span>\n<span class=\"line\"><span>      });</span></span>\n<span class=\"line\"><span>    });</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span>    await page.waitForChanges();</span></span>\n<span class=\"line\"><span>    expect(change).toHaveReceivedEvent();</span></span>\n<span class=\"line\"><span>  });</span></span>\n<span class=\"line\"><span>});</span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span></span></span>\n<span class=\"line\"><span></span></span></code></pre></div><p data-line=\"84\" class=\"code-line\"><code>page.spyOnEvent('myChange')</code> creates a mock object that checks whether the <code>myChange</code> event fired, stored in <code>change</code>. After that, when the event fires, <code>expect(change).toHaveReceivedEvent()</code> passes. (If the event did not fire, <code>toHaveReceivedEvent</code> fails and the test fails.)</p>\n<p data-line=\"86\" class=\"code-line\">Because Puppeteer's <code>select</code> method is unavailable, I use <code>dispatchEvent</code> to force the <code>onChange</code> handler to run. That fires <code>myChange</code>, which the <code>addEventListener</code> was watching, so the Promise inside <code>evaluateHandle</code> resolves and the rest of the test runs.</p>\n<p data-line=\"88\" class=\"code-line\">Shadow DOM made it a bit fiddly, but the test was straightforward to write. Here I only check that the event fired; with <code>toHaveReceivedEventDetail</code> you can also assert the event payload.</p>\n<p data-line=\"90\" class=\"code-line\">See you next time.</p>\n","headings":[{"id":"component-under-test","text":"Component Under Test","level":2},{"id":"test-code","text":"Test Code","level":2}]} as const;
